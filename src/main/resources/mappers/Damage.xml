<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.DamageMapper">
  	<select id="findLatestByUfid" parameterType="String" resultType="map">
    SELECT ufid,
           crack_ins, elecleakage_ins, leakage_ins,
           deformation_ins, anomaly_ins,
           ins_date, inspector
    FROM public.damage_inspect
    WHERE ufid = #{ufid}
    ORDER BY ins_date DESC
    LIMIT 1
  </select> 
	
	
</mapper>