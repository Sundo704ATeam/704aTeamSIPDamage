<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.DamageMapper">
  	<select id="findLatestByUfid" parameterType="int" resultType="map">
    SELECT inscode, managecode,
           crackrate, elecleakrate,
           leakrate, variationrate,
           abnomalityrate, ins_date, inspactor
    FROM public.damage_inspect
    WHERE managecode = #{value}
    ORDER BY ins_date DESC
    LIMIT 1
  </select> 
	
	
</mapper>